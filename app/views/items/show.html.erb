<h1><%= @item.name %></h1>

<ul>
    <li>Price: <%= @item.price %> rubbles.</li>
    <li><%= urls_to_links(urls_to_images(@item.description)) %></li>
    <li>Weight: <%= @item.weight %> kilos.</li>
</ul>

<%= render partial: "shared/email" %>